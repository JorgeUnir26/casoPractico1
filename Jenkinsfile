pipeline {
    agent any

    stages {

        stage('Iniciando Caso Practico 1.2 | Reto 1 - Pipeline CI') {
            steps {
                echo 'Iniciando pipeline para caso practico 1.2'
            }
        }

        stage('Ver workspace') {
            steps {
                bat 'dir'
            }
        }
    }
}
